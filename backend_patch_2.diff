<<<<<<< SEARCH
import { onObjectFinalized } from "firebase-functions/v2/storage";
import * as admin from "firebase-admin";
import { VertexAI } from "@google-cloud/vertexai";
import { ProjectSchema, UnitSchema } from "./schema";
=======
import { onObjectFinalized } from "firebase-functions/v2/storage";
import * as admin from "firebase-admin";
import { VertexAI } from "@google-cloud/vertexai";
import { ProjectSchema, UnitSchema } from "./schema";
import { fromPath } from "pdf2pic";
import * as fs from "fs";
import * as path from "path";
import * as os from "os";
>>>>>>> REPLACE
<<<<<<< SEARCH
  console.log(`Processing PDF: gs://${fileBucket}/${filePath}`);
  const gsUri = `gs://${fileBucket}/${filePath}`;

  // Extract just the filename without the 'b2b_pdfs/' prefix
  const fileName = filePath.split("/").pop();

  if (fileName) {
    try {
      await db.collection("pdf_jobs").doc(fileName).update({
        status: "Processing",
      });
    } catch (e) {
      console.log("Could not update pdf_jobs to Processing, moving on...", e);
    }
  }

  try {
=======
  console.log(`Processing PDF: gs://${fileBucket}/${filePath}`);
  const gsUri = `gs://${fileBucket}/${filePath}`;

  // Extract just the filename without the 'b2b_pdfs/' prefix
  const fileName = filePath.split("/").pop();

  if (fileName) {
    try {
      await db.collection("pdf_jobs").doc(fileName).update({
        status: "Processing",
      });
    } catch (e) {
      console.log("Could not update pdf_jobs to Processing, moving on...", e);
    }
  }

  // Download PDF and extract image
  let heroImageUrl: string | null = null;
  const tempPdfPath = path.join(os.tmpdir(), fileName || "temp.pdf");

  try {
      await admin.storage().bucket(fileBucket).file(filePath).download({ destination: tempPdfPath });

      const options = {
          density: 150,
          saveFilename: `${fileName}_page1`,
          savePath: os.tmpdir(),
          format: "png",
          width: 800,
          height: 800
      };
      const storeAsImage = fromPath(tempPdfPath, options);
      const resolve = await storeAsImage(1);

      if (resolve && resolve.path) {
          const imagePath = resolve.path;
          const imageFileName = path.basename(imagePath);
          const destPath = `b2b_assets/${imageFileName}`;

          await admin.storage().bucket(fileBucket).upload(imagePath, {
              destination: destPath,
              metadata: {
                  contentType: 'image/png'
              }
          });

          console.log(`Hero image uploaded to gs://${fileBucket}/${destPath}`);

          // Get signed URL for public access or use gs:// URI. We'll use gs:// URI to keep it consistent
          // Wait, the requirement says "Map and save these Storage URLs into the assets object".
          // We can use a gs:// URL and use getDownloadURL in the frontend, or get a public URL directly.
          // Let's use the file reference string so frontend can fetch it.
          heroImageUrl = destPath; // Use the path relative to bucket like we do with 'source'

          try {
              fs.unlinkSync(imagePath);
          } catch(e) {}
      }
  } catch (error) {
      console.error("Failed to extract visual assets:", error);
  } finally {
      try {
          fs.unlinkSync(tempPdfPath);
      } catch(e) {}
  }

  try {
>>>>>>> REPLACE
<<<<<<< SEARCH
    const projectId = matchedProjectId || db.collection("projects").doc().id;
    projectData.id = projectId;

    // Coordinate Fallback Logic for Project
=======
    const projectId = matchedProjectId || db.collection("projects").doc().id;
    projectData.id = projectId;

    if (heroImageUrl) {
        if (!projectData.assets) {
            projectData.assets = {};
        }
        if (!projectData.assets.hero_images) {
            projectData.assets.hero_images = [];
        }
        projectData.assets.hero_images.push(heroImageUrl);
    }

    // Coordinate Fallback Logic for Project
>>>>>>> REPLACE
