rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /properties/{document=**} {
      allow read, write: if request.auth != null;
    }
    match /TargetURLs/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}